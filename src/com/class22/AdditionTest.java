package com.class22;

public class AdditionTest {
	/*
	 * Method overloading is a feature that allows a class to have more than one
	 * method in the same class only if: number of parameters are different, 2. the
	 * type of the data that goes in the parameter are different 3. or the sequence
	 * of the parameters are different.
	 */

	Addition obj=new Addition;
	obj.add(10.99, 10.99);
	obj.add(10, 10.99);
	obj.add(10, 10, 10);

}
